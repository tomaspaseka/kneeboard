<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Kneeboard.ViewModels"
             xmlns:models="clr-namespace:Kneeboard.Models"
             x:Class="Kneeboard.Views.WelcomePage"
             x:Name="Root"
             x:DataType="vm:WelcomeViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#111827">

    <Grid RowDefinitions="*,Auto,*">

        <Label Grid.Row="2"
               Text="{Binding AppVersion}"
               FontSize="11"
               TextColor="#6B7280"
               HorizontalOptions="Center"
               VerticalOptions="End"
               Margin="0,0,0,16" />

        <VerticalStackLayout Grid.Row="1"
                             HorizontalOptions="Center"
                             Spacing="16">

            <Label Text="✈ KNEEBOARD"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="#E94560"
                   HorizontalOptions="Center"
                   CharacterSpacing="3" />

            <Label Text="Ready for preflight"
                   FontSize="13"
                   TextColor="#6B7280"
                   HorizontalOptions="Center" />

            <CollectionView ItemsSource="{Binding RecentDocuments}"
                             IsVisible="{Binding HasRecentDocuments}"
                             HorizontalOptions="Center"
                             WidthRequest="320"
                             Margin="0,8,0,0">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:RecentDocument">
                        <Border BackgroundColor="#111827"
                                Stroke="#6B7280"
                                StrokeThickness="1"
                                Margin="0,4"
                                Padding="16,10">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="6" />
                            </Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={x:Reference Root}, Path=BindingContext.OpenRecentCommand}"
                                                       CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <Label Text="{Binding Title}"
                                   TextColor="#E94560"
                                   FontSize="14"
                                   HorizontalTextAlignment="Center" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="OPEN KNEEBOARD FILE…"
                    Command="{Binding OpenFileCommand}"
                    IsEnabled="{Binding IsNotBusy}"
                    BackgroundColor="#E94560"
                    TextColor="White"
                    FontAttributes="Bold"
                    CharacterSpacing="1"
                    CornerRadius="6"
                    Padding="28,10"
                    Margin="0,12,0,0" />

            <Label Text="{Binding ErrorMessage}"
                   IsVisible="{Binding HasError}"
                   TextColor="#F87171"
                   FontSize="13"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   MaximumWidthRequest="400" />

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="#E94560"
                               HorizontalOptions="Center" />

        </VerticalStackLayout>

    </Grid>

</ContentPage>
